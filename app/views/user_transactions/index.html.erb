

<div class="container">
  <h1 class="mb-4">My Transactions</h1>
  <%= render "shared/breadcrumbs" %>
  <div class="d-flex flex-column flex-sm-row flex-sm-wrap" id="user_transactions">
    <% @user_transactions.each do |user_transaction| %>
      <%= render user_transaction %>
    <% end %>
  </div>
  <div class="d-flex flex-column justify-content-center align-items-center my-4 gap-4">
    <%= paginate @user_transactions %>
    <%= link_to "Add Transaction", new_transaction_path(username: current_user.username), class: "btn btn-secondary btn-lg mb-4" %>
  </div>


  <% @credit_cards.each do |credit_card| %>
    
      <%= render "credit_cards/credit_card", credit_card: credit_card, total: @credit_card_totals[credit_card.id] %>

  <% end %>
</div>
