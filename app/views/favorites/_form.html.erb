<%= form_with(model: favorite) do |form| %>
  <% if favorite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>
      <ul>
        <% favorite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :user_id, value: current_user.id %>  
  </div>

  <div>
    <%= form.hidden_field :credit_card_id, value: credit_card_id %>
  </div>

  <div>
    <%= form.button type: "submit", class: "btn btn-light create-star-button" do %>
      <i class="far fa-star"></i> <!-- Empty star icon -->
    <% end %>
  </div>
<% end %>
